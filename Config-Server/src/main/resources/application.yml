spring:
  application:
    name: Config-Server
  cloud:
    config:
      server:
        git:
          uri: https://github.com/Shuvradeep-Datta/Micro-ConfigServer

server:
  port: 8888

eureka:
  client:
    service-url:
      defaultZone: http://localhost:9090/eureka/
